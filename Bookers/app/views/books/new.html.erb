<h2>New book</h2>
<!-- form_for -->
<%= form_for(@book,book_path) do |f| %>

  <p>
  	<%= f.label :title %>
    <%= f.text_field :title %>
  </p>

  <p>
  	<%= f.label :body %>
    <%= f.text_area :body %>
  </p>

  <%= f.submit 'Create Book' %>

<% end %>
